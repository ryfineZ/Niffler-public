-- Generated by aether-data-schema from schema/logical/*.toml.
-- Do not edit generated files directly; edit logical schema or explicit overrides instead.

CREATE TABLE IF NOT EXISTS niffler_upstream_services (
    id TEXT PRIMARY KEY NOT NULL,
    display_name TEXT NOT NULL,
    service_kind TEXT NOT NULL,
    default_api_format TEXT,
    base_url TEXT,
    cost_multiplier REAL NOT NULL DEFAULT 1,
    is_active INTEGER NOT NULL DEFAULT 1,
    config TEXT,
    created_at_unix_ms INTEGER NOT NULL,
    updated_at_unix_ms INTEGER NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_niffler_upstream_services_kind_active ON niffler_upstream_services (service_kind, is_active);

CREATE TABLE IF NOT EXISTS niffler_upstream_accounts (
    id TEXT PRIMARY KEY NOT NULL,
    upstream_service_id TEXT NOT NULL,
    display_name TEXT NOT NULL,
    email TEXT,
    phone TEXT,
    auth_kind TEXT NOT NULL,
    status TEXT NOT NULL DEFAULT 'available',
    cost_multiplier REAL NOT NULL DEFAULT 1,
    priority INTEGER NOT NULL DEFAULT 0,
    cooldown_until_unix_ms INTEGER,
    last_tested_at_unix_ms INTEGER,
    last_test_error TEXT,
    config TEXT,
    created_at_unix_ms INTEGER NOT NULL,
    updated_at_unix_ms INTEGER NOT NULL,
    CONSTRAINT fk_niffler_upstream_accounts_service FOREIGN KEY (upstream_service_id) REFERENCES niffler_upstream_services (id)
);
CREATE INDEX IF NOT EXISTS idx_niffler_upstream_accounts_service_status ON niffler_upstream_accounts (upstream_service_id, status, priority);
CREATE INDEX IF NOT EXISTS idx_niffler_upstream_accounts_email ON niffler_upstream_accounts (email);

CREATE TABLE IF NOT EXISTS niffler_product_plans (
    id TEXT PRIMARY KEY NOT NULL,
    display_name TEXT NOT NULL,
    is_public INTEGER NOT NULL DEFAULT 0,
    is_active INTEGER NOT NULL DEFAULT 1,
    sales_multiplier REAL NOT NULL DEFAULT 1,
    description TEXT,
    created_at_unix_ms INTEGER NOT NULL,
    updated_at_unix_ms INTEGER NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_niffler_product_plans_public_active ON niffler_product_plans (is_public, is_active);

CREATE TABLE IF NOT EXISTS niffler_product_plan_models (
    id TEXT PRIMARY KEY NOT NULL,
    product_plan_id TEXT NOT NULL,
    model_name TEXT NOT NULL,
    is_enabled INTEGER NOT NULL DEFAULT 1,
    sales_multiplier_override REAL,
    created_at_unix_ms INTEGER NOT NULL,
    updated_at_unix_ms INTEGER NOT NULL,
    UNIQUE (product_plan_id, model_name),
    CONSTRAINT fk_niffler_product_plan_models_plan FOREIGN KEY (product_plan_id) REFERENCES niffler_product_plans (id)
);
CREATE INDEX IF NOT EXISTS idx_niffler_product_plan_models_model ON niffler_product_plan_models (model_name, is_enabled);

CREATE TABLE IF NOT EXISTS niffler_model_base_prices (
    id TEXT PRIMARY KEY NOT NULL,
    model_name TEXT NOT NULL,
    input_price_per_million REAL NOT NULL DEFAULT 0,
    output_price_per_million REAL NOT NULL DEFAULT 0,
    cache_write_price_per_million REAL,
    cache_read_price_per_million REAL,
    source TEXT NOT NULL,
    effective_from_unix_ms INTEGER NOT NULL,
    created_at_unix_ms INTEGER NOT NULL,
    updated_at_unix_ms INTEGER NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_niffler_model_base_prices_model_effective ON niffler_model_base_prices (model_name, effective_from_unix_ms);

CREATE TABLE IF NOT EXISTS niffler_upstream_model_prices (
    id TEXT PRIMARY KEY NOT NULL,
    upstream_service_id TEXT NOT NULL,
    model_name TEXT NOT NULL,
    upstream_input_price_per_million REAL,
    upstream_output_price_per_million REAL,
    upstream_cache_write_price_per_million REAL,
    upstream_cache_read_price_per_million REAL,
    price_source_preference TEXT NOT NULL DEFAULT 'official',
    source TEXT,
    synced_at_unix_ms INTEGER,
    created_at_unix_ms INTEGER NOT NULL,
    updated_at_unix_ms INTEGER NOT NULL,
    UNIQUE (upstream_service_id, model_name),
    CONSTRAINT fk_niffler_upstream_model_prices_service FOREIGN KEY (upstream_service_id) REFERENCES niffler_upstream_services (id)
);

CREATE TABLE IF NOT EXISTS niffler_account_model_capabilities (
    id TEXT PRIMARY KEY NOT NULL,
    upstream_service_id TEXT NOT NULL,
    upstream_account_id TEXT NOT NULL,
    model_name TEXT NOT NULL,
    is_enabled INTEGER NOT NULL DEFAULT 1,
    source TEXT,
    last_checked_at_unix_ms INTEGER,
    last_error TEXT,
    created_at_unix_ms INTEGER NOT NULL,
    updated_at_unix_ms INTEGER NOT NULL,
    UNIQUE (upstream_account_id, model_name),
    CONSTRAINT fk_niffler_account_model_capabilities_service FOREIGN KEY (upstream_service_id) REFERENCES niffler_upstream_services (id),
    CONSTRAINT fk_niffler_account_model_capabilities_account FOREIGN KEY (upstream_account_id) REFERENCES niffler_upstream_accounts (id)
);
CREATE INDEX IF NOT EXISTS idx_niffler_account_model_capabilities_model ON niffler_account_model_capabilities (model_name, is_enabled);

CREATE TABLE IF NOT EXISTS niffler_route_attempts (
    id TEXT PRIMARY KEY NOT NULL,
    request_id TEXT NOT NULL,
    upstream_service_id TEXT,
    upstream_account_id TEXT,
    product_plan_id TEXT,
    model_name TEXT NOT NULL,
    attempt_index INTEGER NOT NULL,
    status TEXT NOT NULL,
    skip_reason TEXT,
    upstream_status_code INTEGER,
    latency_ms INTEGER,
    created_at_unix_ms INTEGER NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_niffler_route_attempts_request ON niffler_route_attempts (request_id, attempt_index);
CREATE INDEX IF NOT EXISTS idx_niffler_route_attempts_account ON niffler_route_attempts (upstream_account_id, created_at_unix_ms);

CREATE TABLE IF NOT EXISTS niffler_error_return_settings (
    id TEXT PRIMARY KEY NOT NULL,
    scope TEXT NOT NULL,
    upstream_service_id TEXT,
    match_status_code INTEGER,
    match_text TEXT,
    handling_step TEXT,
    response_mode TEXT NOT NULL DEFAULT 'replace',
    user_message TEXT NOT NULL,
    account_protection_action TEXT NOT NULL DEFAULT 'record_only',
    pause_duration TEXT,
    is_active INTEGER NOT NULL DEFAULT 1,
    created_at_unix_ms INTEGER NOT NULL,
    updated_at_unix_ms INTEGER NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_niffler_error_return_settings_scope_active ON niffler_error_return_settings (scope, is_active);
CREATE INDEX IF NOT EXISTS idx_niffler_error_return_settings_upstream ON niffler_error_return_settings (upstream_service_id, is_active);

CREATE TABLE IF NOT EXISTS niffler_account_risk_events (
    id TEXT PRIMARY KEY NOT NULL,
    upstream_service_id TEXT,
    upstream_account_id TEXT NOT NULL,
    request_id TEXT,
    user_id TEXT,
    api_key_id TEXT,
    model_name TEXT,
    rule_id TEXT,
    matched_text TEXT,
    upstream_status_code INTEGER,
    action TEXT NOT NULL,
    created_at_unix_ms INTEGER NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_niffler_account_risk_events_account_time ON niffler_account_risk_events (upstream_account_id, created_at_unix_ms);
CREATE INDEX IF NOT EXISTS idx_niffler_account_risk_events_request ON niffler_account_risk_events (request_id);

CREATE TABLE IF NOT EXISTS niffler_api_key_pauses (
    id TEXT PRIMARY KEY NOT NULL,
    api_key_id TEXT NOT NULL,
    reason_code TEXT NOT NULL,
    user_message TEXT NOT NULL,
    paused_until_unix_ms INTEGER,
    manual_restore_required INTEGER NOT NULL DEFAULT 0,
    created_at_unix_ms INTEGER NOT NULL,
    restored_at_unix_ms INTEGER,
    restored_by TEXT
);
CREATE INDEX IF NOT EXISTS idx_niffler_api_key_pauses_key_active ON niffler_api_key_pauses (api_key_id, restored_at_unix_ms, paused_until_unix_ms);

CREATE TABLE IF NOT EXISTS niffler_upstream_service_capabilities (
    id TEXT PRIMARY KEY NOT NULL,
    upstream_service_id TEXT NOT NULL,
    protocol_kind TEXT NOT NULL,
    capability_kind TEXT NOT NULL,
    is_enabled INTEGER NOT NULL DEFAULT 1,
    config TEXT,
    created_at_unix_ms INTEGER NOT NULL,
    updated_at_unix_ms INTEGER NOT NULL,
    UNIQUE (upstream_service_id, protocol_kind, capability_kind),
    CONSTRAINT fk_niffler_upstream_service_capabilities_service FOREIGN KEY (upstream_service_id) REFERENCES niffler_upstream_services (id)
);
CREATE INDEX IF NOT EXISTS idx_niffler_upstream_service_capabilities_lookup ON niffler_upstream_service_capabilities (protocol_kind, capability_kind, is_enabled);

CREATE TABLE IF NOT EXISTS niffler_settlement_snapshots (
    id TEXT PRIMARY KEY NOT NULL,
    request_id TEXT NOT NULL,
    user_id TEXT,
    api_key_id TEXT,
    product_plan_id TEXT,
    upstream_service_id TEXT,
    upstream_account_id TEXT,
    requested_model_name TEXT NOT NULL,
    upstream_execution_model_name TEXT,
    image_tool_model_name TEXT,
    pricing_snapshot TEXT NOT NULL,
    wallet_charge_usd REAL NOT NULL DEFAULT 0,
    entitlement_charge_usd REAL NOT NULL DEFAULT 0,
    upstream_cost_usd REAL NOT NULL DEFAULT 0,
    gross_margin_usd REAL NOT NULL DEFAULT 0,
    created_at_unix_ms INTEGER NOT NULL,
    finalized_at_unix_ms INTEGER,
    UNIQUE (request_id)
);
CREATE INDEX IF NOT EXISTS idx_niffler_settlement_snapshots_user_time ON niffler_settlement_snapshots (user_id, created_at_unix_ms);

CREATE TABLE IF NOT EXISTS niffler_billing_reservations (
    id TEXT PRIMARY KEY NOT NULL,
    request_id TEXT NOT NULL,
    user_id TEXT,
    api_key_id TEXT,
    product_plan_id TEXT,
    status TEXT NOT NULL,
    reserved_total_usd REAL NOT NULL DEFAULT 0,
    wallet_reserved_usd REAL NOT NULL DEFAULT 0,
    entitlement_reserved_usd REAL NOT NULL DEFAULT 0,
    reserved_at_unix_ms INTEGER NOT NULL,
    expires_at_unix_ms INTEGER NOT NULL,
    finalized_at_unix_ms INTEGER,
    settlement_snapshot_id TEXT,
    release_reason TEXT,
    idempotency_key TEXT NOT NULL,
    UNIQUE (request_id),
    UNIQUE (idempotency_key),
    CONSTRAINT fk_niffler_billing_reservations_snapshot FOREIGN KEY (settlement_snapshot_id) REFERENCES niffler_settlement_snapshots (id)
);
CREATE INDEX IF NOT EXISTS idx_niffler_billing_reservations_status_expires ON niffler_billing_reservations (status, expires_at_unix_ms);
CREATE INDEX IF NOT EXISTS idx_niffler_billing_reservations_user_time ON niffler_billing_reservations (user_id, reserved_at_unix_ms);

CREATE TABLE IF NOT EXISTS niffler_billing_reservation_events (
    id TEXT PRIMARY KEY NOT NULL,
    reservation_id TEXT NOT NULL,
    event_kind TEXT NOT NULL,
    amount_usd REAL NOT NULL DEFAULT 0,
    reason TEXT,
    idempotency_key TEXT NOT NULL,
    actor_id TEXT,
    created_at_unix_ms INTEGER NOT NULL,
    UNIQUE (reservation_id, idempotency_key),
    CONSTRAINT fk_niffler_billing_reservation_events_reservation FOREIGN KEY (reservation_id) REFERENCES niffler_billing_reservations (id)
);
CREATE INDEX IF NOT EXISTS idx_niffler_billing_reservation_events_reservation ON niffler_billing_reservation_events (reservation_id, created_at_unix_ms);

CREATE TABLE IF NOT EXISTS niffler_referral_reward_rules (
    id TEXT PRIMARY KEY NOT NULL,
    display_name TEXT NOT NULL,
    status TEXT NOT NULL DEFAULT 'active',
    reward_kind TEXT NOT NULL,
    reward_value REAL NOT NULL,
    applies_to_order_kind TEXT,
    max_reward_usd REAL,
    effective_from_unix_ms INTEGER NOT NULL,
    effective_until_unix_ms INTEGER,
    config TEXT,
    created_at_unix_ms INTEGER NOT NULL,
    updated_at_unix_ms INTEGER NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_niffler_referral_reward_rules_status_time ON niffler_referral_reward_rules (status, effective_from_unix_ms, effective_until_unix_ms);

CREATE TABLE IF NOT EXISTS niffler_referral_reward_ledger (
    id TEXT PRIMARY KEY NOT NULL,
    order_id TEXT NOT NULL,
    idempotency_key TEXT NOT NULL,
    inviter_user_id TEXT NOT NULL,
    invitee_user_id TEXT NOT NULL,
    rule_id TEXT,
    reward_amount_usd REAL NOT NULL,
    rule_snapshot TEXT NOT NULL,
    status TEXT NOT NULL,
    failure_reason TEXT,
    retry_count INTEGER NOT NULL DEFAULT 0,
    paid_at_unix_ms INTEGER,
    cancelled_at_unix_ms INTEGER,
    created_at_unix_ms INTEGER NOT NULL,
    updated_at_unix_ms INTEGER NOT NULL,
    UNIQUE (order_id),
    UNIQUE (idempotency_key),
    CONSTRAINT fk_niffler_referral_reward_ledger_rule FOREIGN KEY (rule_id) REFERENCES niffler_referral_reward_rules (id)
);
CREATE INDEX IF NOT EXISTS idx_niffler_referral_reward_ledger_inviter ON niffler_referral_reward_ledger (inviter_user_id, created_at_unix_ms);
CREATE INDEX IF NOT EXISTS idx_niffler_referral_reward_ledger_status ON niffler_referral_reward_ledger (status, updated_at_unix_ms);

CREATE TABLE IF NOT EXISTS niffler_referral_reward_events (
    id TEXT PRIMARY KEY NOT NULL,
    reward_ledger_id TEXT NOT NULL,
    event_kind TEXT NOT NULL,
    reason TEXT,
    actor_id TEXT,
    event_snapshot TEXT,
    created_at_unix_ms INTEGER NOT NULL,
    CONSTRAINT fk_niffler_referral_reward_events_ledger FOREIGN KEY (reward_ledger_id) REFERENCES niffler_referral_reward_ledger (id)
);
CREATE INDEX IF NOT EXISTS idx_niffler_referral_reward_events_ledger ON niffler_referral_reward_events (reward_ledger_id, created_at_unix_ms);
